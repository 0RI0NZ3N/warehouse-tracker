# Tracker

Internal tool. Not for public use.

## Setup

Deploy as a static site over HTTPS (required for camera access). Open the
deployed URL on the target device in Chrome, then use the browser menu to
add it to the home screen.

Data stays on-device only; nothing is transmitted anywhere unless
explicitly exported from within the app.
